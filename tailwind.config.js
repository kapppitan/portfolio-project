module.exports = {
    theme: {
        extend: {
            fontFamily: {
                customFont: ['Modulus']
            },
            cursor: {
                default: 'url("cursor.png"), default',
                pointer: 'url("clicker.png"), pointer',
            }
        },
    },
    plugins: [],
}